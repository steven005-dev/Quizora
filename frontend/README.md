
  # Quizora application design prompt

  This is a code bundle for Quizora application design prompt. The original project is available at https://www.figma.com/design/ONT7eBBWqzyC0pQDejjBOh/Quizora-application-design-prompt.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  